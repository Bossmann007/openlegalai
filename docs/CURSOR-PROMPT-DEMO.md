# Prompt Cursor — DEMO ONLY (OpenLegalAI)

A demo já está no repo (`client/` + `server/`).

Layout do server = DNA do [Buglan](https://github.com/joaozupeli/buglan) (`common`, `config`, `models`, `modules`, aliases `@modules/*`, prefixo `api`).

Produto: número do processo obrigatório → capa fixture → juris com votos → dissídio de câmaras → chance/blindagem.

Não implementar ingestão DataJud de bilhões de linhas. Não usar Jusbrasil.
